# Upsun configuration, generated using AI at: 2026-09-14T17:11:12.624Z
# AI can make mistakes. Please modify this file to suit your needs.
applications:
  remux-server:
    type: rust:1
    source:
      root: crates/remux-server
    hooks:
      build: |
        set -ex
        cargo build --release
    web:
      commands:
        start: ./target/release/remux-server
      locations:
        /:
          allow: false
          passthru: true
    relationships:
      database: {}

services:
  database:
    type: postgresql:18

routes:
  https://{default}/:
    type: upstream
    upstream: remux-server:http
